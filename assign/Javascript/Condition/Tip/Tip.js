let price = 3000;

if(price >= 5000 && price <= 30000){
   let pay = (price*15)/100;
    console.log(pay + price)
}else{
   let pay = (price*20)/100;
    console.log(pay + price)
}

// let pay = price >= 5000 ; price <= 30000 ? ((pay = (price*15)/100 ) (console.log(pay + price))) : ((pay = (price*20)/100), (console.log(pay + price)))
